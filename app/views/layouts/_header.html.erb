<header>
    <div class="container">
        <nav>
          <ul id="chart_menu" class="nav nav-tabs">
                <li role="presentation"><%= link_to "ダイヤモンドチャート", root_path %></li>
                <li role="presentation"><%= link_to "ダイヤモンドチャート($ドル)", tables_chart_second_path %></li>
                <li role="presentation"><%= link_to "ダイヤモンド取引所価格指標", lists_show_path %></li>
          </ul>
          
          <ul class="nav navbar-nav navbar-right">  
            <% if logged_in? %>
              <% if current_user.id == 1 || current_user.id == 2 %>
                <li class="hidden-xs"><%= link_to "データ管理", diamonds_manage_data_path %></li>
                <li class="hidden-xs"><%= link_to "Log out", logout_path, method: "delete" %></l1>
              <% else %>
                <li class="hidden-xs">
                <%= link_to "Log out", logout_path, method: "delete" %>
                </li>
              <% end %>
            <% else %>
            <li class="hidden-xs"><%= link_to "Log in", login_path %></li>
            <% end %>
          </ul>
        </nav>
    
    </div>
</header>